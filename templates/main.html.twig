{% extends 'base.html.twig' %}

{% block stylesheets %}
    <style>
        .table th, td{
            text-align: center;
        }
    </style>
{% endblock %}

{% block navbar %}
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
            <a class="navbar-brand" href="{{ path('restaurant') }}">{{ 'Home'|trans }}</a>
            <a class="navbar-brand" href="{{ path('restaurant_add') }}">{{ 'Add restaurant'|trans }}</a>
            <ul class="navbar-nav ml-auto">
                <li><span class="navbar-text">{{ app.user.email }}</span></li>
                <li class="ml-2"><span class="navbar-text">API token: {{ app.user.apiToken }}</span></li>
                <li class="ml-2"><a class="navbar-brand" href="{{ path('logout') }}">{{ 'Logout'|trans }}</a></li>
            </ul>
    </nav>
{% endblock %}